<UserControl x:Class="PromptRunner.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PromptRunner.Views"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#ECF0F1">
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="実行ログ履歴" FontSize="28" FontWeight="Bold"
                      Foreground="#2C3E50" Margin="0,0,0,10"/>

            <!-- Status and Controls -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}"
                          FontSize="14" Foreground="#7F8C8D" VerticalAlignment="Center"/>

                <Button Grid.Column="1" Content="すべてのログを削除"
                       Command="{Binding ClearAllLogsCommand}"
                       Padding="15,8" Margin="5" FontSize="12"
                       Background="#E74C3C" Foreground="White"
                       BorderThickness="0" Cursor="Hand"/>
            </Grid>

            <!-- Log List and Detail -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Log List -->
                <Border Grid.Column="0" Background="White" BorderBrush="#BDC3C7"
                       BorderThickness="1" CornerRadius="5" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="ログ一覧" FontSize="14" FontWeight="Bold"
                                  Foreground="#2C3E50" Margin="15,10" />

                        <ListBox Grid.Row="1" ItemsSource="{Binding Logs}"
                                SelectedItem="{Binding SelectedLog}"
                                BorderThickness="0" Margin="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#ECF0F1" BorderThickness="0,0,0,1" Padding="10">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Text="{Binding ExecutedAt, StringFormat='yyyy/MM/dd HH:mm:ss'}"
                                                      FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>

                                            <TextBlock Grid.Row="1" FontSize="11" Foreground="#7F8C8D" Margin="0,3,0,0">
                                                <Run Text="{Binding Items.Count, Mode=OneWay}"/>
                                                <Run Text="項目実行"/>
                                            </TextBlock>

                                            <WrapPanel Grid.Row="2" Margin="0,5,0,0">
                                                <Button Content="エクスポート"
                                                       Command="{Binding DataContext.ExportLogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding}"
                                                       Padding="8,4" Margin="0,0,5,0" FontSize="10"
                                                       Background="#3498DB" Foreground="White"
                                                       BorderThickness="0" Cursor="Hand"/>

                                                <Button Content="削除"
                                                       Command="{Binding DataContext.DeleteLogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding}"
                                                       Padding="8,4" FontSize="10"
                                                       Background="#E74C3C" Foreground="White"
                                                       BorderThickness="0" Cursor="Hand"/>
                                            </WrapPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Log Detail -->
                <Border Grid.Column="1" Background="White" BorderBrush="#BDC3C7"
                       BorderThickness="1" CornerRadius="5" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Toggle Markdown View Button -->
                        <Button Grid.Row="0" Command="{Binding ToggleMarkdownViewCommand}"
                               Padding="10,6" Margin="0,0,0,10" FontSize="12"
                               Background="#8E44AD" Foreground="White"
                               BorderThickness="0" Cursor="Hand"
                               HorizontalAlignment="Right"
                               Visibility="{Binding SelectedLog, Converter={StaticResource NullToVisibilityConverter}}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Content" Value="📝 プレーンテキスト"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMarkdownView}" Value="False">
                                            <Setter Property="Content" Value="📄 マークダウン"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- Plain Text View -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ScrollViewer.Style>
                                <Style TargetType="ScrollViewer">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMarkdownView}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ScrollViewer.Style>
                            <StackPanel>
                            <TextBlock Text="{Binding SelectedLog.ExecutedAt, StringFormat='実行日時: {0:yyyy年MM月dd日 HH:mm:ss}'}"
                                      FontSize="16" FontWeight="Bold" Foreground="#2C3E50"
                                      Margin="0,0,0,15"
                                      Visibility="{Binding SelectedLog, Converter={StaticResource NullToVisibilityConverter}}"/>

                            <!-- Input Text -->
                            <TextBlock Text="入力テキスト" FontSize="14" FontWeight="Bold"
                                      Foreground="#34495E" Margin="0,0,0,5"
                                      Visibility="{Binding SelectedLog, Converter={StaticResource NullToVisibilityConverter}}"/>
                            <Border Background="#F8F9FA" BorderBrush="#DEE2E6"
                                   BorderThickness="1" CornerRadius="3" Padding="10" Margin="0,0,0,15"
                                   Visibility="{Binding SelectedLog, Converter={StaticResource NullToVisibilityConverter}}">
                                <TextBox Text="{Binding SelectedLog.InputText, Mode=OneWay}"
                                        FontSize="12" Foreground="#495057" TextWrapping="Wrap"
                                        IsReadOnly="True" BorderThickness="0" Background="Transparent"/>
                            </Border>

                            <!-- Extracted Facts -->
                            <TextBlock Text="抽出された事実" FontSize="14" FontWeight="Bold"
                                      Foreground="#34495E" Margin="0,0,0,5"
                                      Visibility="{Binding SelectedLog.ExtractedFacts, Converter={StaticResource StringToVisibilityConverter}}"/>
                            <Border Background="#F8F9FA" BorderBrush="#DEE2E6"
                                   BorderThickness="1" CornerRadius="3" Padding="10" Margin="0,0,0,15"
                                   Visibility="{Binding SelectedLog.ExtractedFacts, Converter={StaticResource StringToVisibilityConverter}}">
                                <TextBox Text="{Binding SelectedLog.ExtractedFacts, Mode=OneWay}"
                                        FontSize="12" Foreground="#495057" TextWrapping="Wrap"
                                        IsReadOnly="True" BorderThickness="0" Background="Transparent"/>
                            </Border>

                            <!-- Execution Results -->
                            <TextBlock Text="実行結果" FontSize="14" FontWeight="Bold"
                                      Foreground="#34495E" Margin="0,0,0,10"
                                      Visibility="{Binding SelectedLog, Converter={StaticResource NullToVisibilityConverter}}"/>

                            <ItemsControl ItemsSource="{Binding SelectedLog.Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#ECF0F1" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                                            <StackPanel>
                                                <TextBlock Text="{Binding ItemTitle}" FontSize="13" FontWeight="Bold"
                                                          Foreground="#2C3E50" Margin="0,0,0,8"/>

                                                <!-- Prompt Template -->
                                                <TextBlock Text="使用したプロンプトテンプレート" FontSize="11" FontWeight="SemiBold"
                                                          Foreground="#6C757D" Margin="0,0,0,5"/>
                                                <Border Background="#F1F3F4" BorderBrush="#CED4DA"
                                                       BorderThickness="1" CornerRadius="3" Padding="8" Margin="0,0,0,8">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>

                                                        <TextBox Grid.Row="0" Text="{Binding PromptTemplate, Mode=OneWay}"
                                                                TextWrapping="Wrap" IsReadOnly="True"
                                                                FontSize="10" FontFamily="Consolas"
                                                                Background="Transparent" BorderThickness="0"
                                                                MaxHeight="100" VerticalScrollBarVisibility="Auto"/>

                                                        <Button Grid.Row="1" Content="プロンプトをコピー"
                                                               Command="{Binding DataContext.CopyPromptCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                               CommandParameter="{Binding}"
                                                               Padding="6,3" Margin="0,5,0,0" FontSize="9"
                                                               Background="#9B59B6" Foreground="White"
                                                               BorderThickness="0" Cursor="Hand"
                                                               HorizontalAlignment="Left"/>
                                                    </Grid>
                                                </Border>

                                                <!-- User Note -->
                                                <StackPanel Visibility="{Binding UserNote, Converter={StaticResource StringToVisibilityConverter}}">
                                                    <TextBlock Text="補足条件" FontSize="11" FontWeight="SemiBold"
                                                              Foreground="#6C757D" Margin="0,0,0,5"/>
                                                    <Border Background="#FFF9E6" BorderBrush="#FFE5B4"
                                                           BorderThickness="1" CornerRadius="3" Padding="8" Margin="0,0,0,8">
                                                        <TextBox Text="{Binding UserNote, Mode=OneWay}" FontSize="11"
                                                                Foreground="#856404" TextWrapping="Wrap"
                                                                IsReadOnly="True" BorderThickness="0" Background="Transparent"/>
                                                    </Border>
                                                </StackPanel>

                                                <!-- Result -->
                                                <TextBlock Text="結果" FontSize="11" FontWeight="SemiBold"
                                                          Foreground="#6C757D" Margin="0,0,0,5"
                                                          Visibility="{Binding IsSuccess, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                <Border Background="White" BorderBrush="#DEE2E6"
                                                       BorderThickness="1" CornerRadius="3" Padding="10"
                                                       Visibility="{Binding IsSuccess, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <TextBox Text="{Binding ResultContent, Mode=OneWay}" FontSize="11"
                                                            Foreground="#212529" TextWrapping="Wrap"
                                                            IsReadOnly="True" BorderThickness="0" Background="Transparent"/>
                                                </Border>

                                                <!-- Error -->
                                                <Border Background="#F8D7DA" BorderBrush="#F5C6CB"
                                                       BorderThickness="1" CornerRadius="3" Padding="10"
                                                       Visibility="{Binding IsSuccess, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                    <StackPanel>
                                                        <TextBlock Text="エラーが発生しました" FontSize="11" FontWeight="Bold"
                                                                  Foreground="#721C24" Margin="0,0,0,5"/>
                                                        <TextBox Text="{Binding ErrorMessage, Mode=OneWay}" FontSize="11"
                                                                Foreground="#721C24" TextWrapping="Wrap"
                                                                IsReadOnly="True" BorderThickness="0" Background="Transparent"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>

                        <!-- Markdown View -->
                        <wv2:WebView2 x:Name="MarkdownWebView" Grid.Row="1">
                            <wv2:WebView2.Style>
                                <Style TargetType="wv2:WebView2">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMarkdownView}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </wv2:WebView2.Style>
                        </wv2:WebView2>
                    </Grid>
                </Border>
            </Grid>

            <!-- Buttons -->
            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Button Content="← 戻る"
                       Command="{Binding BackCommand}"
                       Padding="20,12" Margin="0" FontSize="14"
                       Background="#95A5A6" Foreground="White"
                       BorderThickness="0" Cursor="Hand"
                       HorizontalAlignment="Left"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

<Window x:Class="PromptRunner.Views.ChecklistManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PromptRunner.Views"
        mc:Ignorable="d"
        Title="チェックリスト管理" Height="700" Width="1200"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Background="#ECF0F1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="チェックリスト管理" FontSize="24" FontWeight="Bold"
                      Foreground="#2C3E50" Margin="0,0,0,20"/>

            <!-- Toolbar -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        FontSize="14" Padding="10" Margin="0,0,10,0"
                        BorderBrush="#BDC3C7" BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Foreground" Value="#2C3E50"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="検索 (ID, タイトル, 説明, プロンプト)"
                                                              Foreground="#95A5A6" Margin="5,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="➕ 新規追加" Command="{Binding AddNewCommand}"
                           Padding="15,8" Margin="5,0" FontSize="12"
                           Background="#27AE60" Foreground="White"
                           BorderThickness="0" Cursor="Hand"/>

                    <Button Content="✏️ 編集" Command="{Binding EditCommand}"
                           Padding="15,8" Margin="5,0" FontSize="12"
                           Background="#3498DB" Foreground="White"
                           BorderThickness="0" Cursor="Hand"/>

                    <Button Content="🗑️ 削除" Command="{Binding DeleteCommand}"
                           Padding="15,8" Margin="5,0" FontSize="12"
                           Background="#E74C3C" Foreground="White"
                           BorderThickness="0" Cursor="Hand"/>

                    <Button Content="📤 エクスポート" Command="{Binding ExportCommand}"
                           Padding="15,8" Margin="5,0" FontSize="12"
                           Background="#9B59B6" Foreground="White"
                           BorderThickness="0" Cursor="Hand"/>

                    <Button Content="📥 インポート" Command="{Binding ImportCommand}"
                           Padding="15,8" Margin="5,0" FontSize="12"
                           Background="#E67E22" Foreground="White"
                           BorderThickness="0" Cursor="Hand"/>

                    <Button Content="🔄 デフォルトに戻す" Command="{Binding ResetToDefaultCommand}"
                           Padding="15,8" Margin="5,0" FontSize="12"
                           Background="#95A5A6" Foreground="White"
                           BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- Item List -->
                <Border Grid.Column="0" Background="White" BorderBrush="#BDC3C7"
                       BorderThickness="1" CornerRadius="5" Margin="0,0,10,0">
                    <DataGrid ItemsSource="{Binding Items}"
                             SelectedItem="{Binding SelectedItem}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             BorderThickness="0"
                             GridLinesVisibility="Horizontal"
                             HeadersVisibility="Column"
                             SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="カテゴリ" Binding="{Binding Category}" Width="Auto"/>
                            <DataGridTextColumn Header="タイトル" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Detail/Edit Panel -->
                <Border Grid.Column="1" Background="White" BorderBrush="#BDC3C7"
                       BorderThickness="1" CornerRadius="5" Padding="20">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- View Mode -->
                            <StackPanel Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverted}">
                                <TextBlock Text="項目の詳細" FontSize="18" FontWeight="Bold"
                                          Foreground="#2C3E50" Margin="0,0,0,20"/>

                                <TextBlock Text="選択した項目の詳細が表示されます"
                                          FontSize="14" Foreground="#7F8C8D"
                                          Visibility="{Binding SelectedItem, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverted}"/>

                                <StackPanel Visibility="{Binding SelectedItem, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="ID" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding SelectedItem.Id}" FontSize="14" Margin="0,0,0,15"/>

                                    <TextBlock Text="タイトル" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding SelectedItem.Title}" FontSize="14" Margin="0,0,0,15"/>

                                    <TextBlock Text="説明" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding SelectedItem.Description}" FontSize="14"
                                              TextWrapping="Wrap" Margin="0,0,0,15"/>

                                    <TextBlock Text="カテゴリ" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding SelectedItem.Category}" FontSize="14" Margin="0,0,0,15"/>

                                    <TextBlock Text="プロンプトテンプレート" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                    <TextBox Text="{Binding SelectedItem.PromptTemplate, Mode=OneWay}"
                                            FontSize="12" TextWrapping="Wrap" IsReadOnly="True"
                                            Background="#F8F9FA" BorderBrush="#DEE2E6"
                                            Padding="10" MinHeight="200" MaxHeight="300"
                                            VerticalScrollBarVisibility="Auto"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Edit Mode -->
                            <StackPanel Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="項目の編集" FontSize="18" FontWeight="Bold"
                                          Foreground="#2C3E50" Margin="0,0,0,20"/>

                                <TextBlock Text="ID" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding EditId}" FontSize="14" Padding="8" Margin="0,0,0,15"
                                        IsReadOnly="True" Background="#ECF0F1"/>

                                <TextBlock Text="タイトル *" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}"
                                        FontSize="14" Padding="8" Margin="0,0,0,15"/>

                                <TextBlock Text="説明" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}"
                                        FontSize="14" Padding="8" Margin="0,0,0,15"
                                        TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60"/>

                                <TextBlock Text="カテゴリ *" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding AvailableCategories}"
                                         SelectedItem="{Binding EditCategory}"
                                         FontSize="14" Padding="8" Margin="0,0,0,15"/>

                                <TextBlock Text="プロンプトテンプレート *" FontSize="12" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                <TextBlock FontSize="11" Foreground="#95A5A6" Margin="0,0,0,5">
                                    <Run Text=""/>
                                    <Run Text="InputText を入力テキストに、UserNote をユーザーの補足条件に置き換えます"/>
                                </TextBlock>
                                <TextBox Text="{Binding EditPromptTemplate, UpdateSourceTrigger=PropertyChanged}"
                                        FontSize="12" Padding="10" Margin="0,0,0,15"
                                        TextWrapping="Wrap" AcceptsReturn="True"
                                        MinHeight="200" MaxHeight="300"
                                        VerticalScrollBarVisibility="Auto"/>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="💾 保存" Command="{Binding SaveCommand}"
                                           Padding="20,10" Margin="5,0" FontSize="14"
                                           Background="#27AE60" Foreground="White"
                                           BorderThickness="0" Cursor="Hand"/>

                                    <Button Content="❌ キャンセル" Command="{Binding CancelEditCommand}"
                                           Padding="20,10" Margin="5,0" FontSize="14"
                                           Background="#95A5A6" Foreground="White"
                                           BorderThickness="0" Cursor="Hand"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
